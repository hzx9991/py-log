# 第一天笔记
## python入门
***
输入输出

>输出 print函数
>* print（变量）
>* print（字符串）

>输入 input函数
>* input（）
***
python风格
续行
反斜杠\

>缩进问题
>1. 增加缩进表示语句块的开始
>2. python用相同缩进表示同级别语句块
>3. 减少缩进表示语句块的退出
***
## python语法基础
变量
标识对象——引用对象

>标识符：允许作为变量名或者其他对象名称的有效符号
>- 首字符字母或下划线
>- 其余可以是字母、下划线、数字
>- 大小写敏感

关键字 不可以作为标识符 

> 运算符&表达式
> 乘方**
> 正负号+-
> 赋值等于号

> 赋值
> python是动态的强类型语言
不需要显示声明 根据“值确定”

增量赋值
如 m%=5 m=m%5
链式赋值
PI=pi=3.14
多重赋值
x=1
y=1
x,y (元祖)
x,y=y,x
x,y
多重赋值实际上是 元祖打包 解包的过程

语句：完整执行一个任务的一行逻辑代码
***
python 数据类型
整形，浮点型，布尔型，复叔型，布尔型，字符串，列表，元祖，字典

>整形 interget
>整形和长整形并不严格区分
>整形没有溢出的错误

>布尔型 bool
>整形的子类
>只有两个值 ture false

浮点型 float
数学中的实数，可以用科学计算法

复数型 complex
实数+虚数
2.4+5.6j
复数.real
复数.imag
复数.conjugate()

字符串 不可变
列表[]
元祖()

字符串的表示： 单引号， 双引号，三引号
索引操作符
myString = 'hello'
myString[1] = e

映射类型  字典
用大括号{}界别
类似于哈希表的键值对
***
python 基本运算
算术运算
算术运算符的优先级
乘方 ** 正负号+-
乘除*/ 整除//
取余% 加减+-
乘除大于加减

比较运算 
< > <= >= == !=
小于的结合性 从左到右

逻辑运算符
not非 and与 or或

字符运算符
原始字符串操作符（r/R）
用于一些不希望转义字符起作用的地方
所有的字符串都是UNICODE字符串
f = open(r'c:\python\test.py','w')
f = open(c:\\python\\test.py','w')

位运算 左移右移
综合运算 注意优先级
